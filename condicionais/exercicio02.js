const weather = 'cloudy';
// A FAZER: Definir a atividade com base no tempo usando um operador ternário.
// Se estiver ensolarado, brincar ao ar livre; caso contrário, ficar dentro de casa e jogar jogos.
const activity = weather === 'sunny' ? 'play outside' : 'stay inside and play games';

console.log(activity);
