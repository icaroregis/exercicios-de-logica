// Muito bem, Navegador Espacial! Ajuste o código para registrar o número de estrelas restantes após cada descoberta. Preparar, apontar, já!

let starsToDiscover = 3;

while (starsToDiscover > 0) {
  starsToDiscover--;
  console.log(starsToDiscover);
}
