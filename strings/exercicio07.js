let starLog = 'Vega is bright, but Betelgeuse is brighter!';
console.log(starLog.indexOf('bright')); // Should output the index where 'bright' first appears
console.log(starLog.replaceAll('bright', 'dim')); // Replace all occurrences of 'bright' with 'dim'
