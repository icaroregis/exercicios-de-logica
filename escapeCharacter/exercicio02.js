let bookTitle = "Alice's Adventures in Wonderland";
let famousLine = `The Cheshire Cat grinned, "We're all mad here."`;

// Change the next line to use template strings instead
console.log(`One of the beloved classics is a tale called "${bookTitle}" where a quirky line states:\n\t ${famousLine}`);